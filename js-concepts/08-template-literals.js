// Backtick syntax

let texto = `Isso é uma template literal`


// String interpolation

let nome = "Maria"
let idade = 30

let frase = `Meu nome é ${nome} e tenho ${idade} anos`

let calculo = `2 + 2 = ${2 + 2}`

let condicional = `Status: ${idade >= 18 ? "adulto" : "menor"}`


// Multiline strings

let html = `
    <div class="card">
        <h1>${nome}</h1>
        <p>Idade: ${idade}</p>
    </div>
`

let lista = `
Linha 1
Linha 2
Linha 3
`
